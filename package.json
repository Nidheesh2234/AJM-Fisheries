{
  "name": "ajm-fisheries",
  "version": "1.0.0",
  "description": "Wholesale seafood web application for AJM Fisheries (Supabase BaaS)",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "netlify-cli": "^27.4.1"
  },
  "dependencies": {
    "@studio-freight/lenis": "^1.0.42",
    "gsap": "^3.15.0"
  }
}
